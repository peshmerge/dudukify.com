const app = Vue.createApp({
    date() {
        return {
            message: 'hello world'
        }
    },

    methods: {
        playNote(note) {
            this.playNote();
        }
    }
})  